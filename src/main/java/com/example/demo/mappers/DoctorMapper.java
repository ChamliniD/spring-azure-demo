/*
package com.example.demo.mappers;

import com.example.demo.dtos.DoctorDTO;
import com.example.demo.entities.Doctor;
import org.mapstruct.Mapper;
import org.mapstruct.ReportingPolicy;

import java.util.List;

@Mapper(unmappedTargetPolicy = ReportingPolicy.IGNORE, componentModel = "spring")
public interface DoctorMapper {
    DoctorDTO domainToDto(Doctor doctor);

    List<DoctorDTO> domainToDto(List<Doctor> doctors);

    Doctor dtoToDomain(DoctorDTO doctorDTO);

    List<Doctor> dtoToDomain(List<DoctorDTO> doctorDTOs);

}
*/
